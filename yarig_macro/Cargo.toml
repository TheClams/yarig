# Cargo.toml for the proc macro crate
[package]
name = "yarig_macro"
version = "0.9.0"
edition = "2024"
license = "MIT"
description = "Macro for yarig"

[lib]
proc-macro = true
name = "yarig_macro"

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
